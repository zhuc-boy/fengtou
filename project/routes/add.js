var express = require("express");
var router = express.Router();
var URL = require("url");
var mysql = require("mysql");
var router = express.Router();
var sql = "SELECT * FROM ××"; //×× 表名称
var addsql = "INSERT INTO ××(type,number,price,factory) VALUES(?,?,?,?)";
